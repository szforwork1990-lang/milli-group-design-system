/** Column heading: the mango dot plus a 13pt medium label, as on Main Slide layouts. */
export interface ColumnHeadProps {
  children?: React.ReactNode;
  color?: string;
  style?: React.CSSProperties;
}
export declare function ColumnHead(props: ColumnHeadProps): JSX.Element;
