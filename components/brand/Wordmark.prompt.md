Small typeset "**Milli** Group" lockup for slide chrome, where the vector logo would be too heavy.

```jsx
<Wordmark color="var(--text-on-dark)" />
```

One colour only — "Milli" is bold, " Group" is medium. Use `Logo` instead at any size above ~20px.
