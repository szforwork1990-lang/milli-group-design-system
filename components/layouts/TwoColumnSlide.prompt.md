Default content slide. Reach for this before anything else.

```jsx
<TwoColumnSlide title="Where growth came from" page={4} total={18}
  columns={[{ head: 'Retail', body: <BulletList items={['Up 12% YoY']} /> },
            { head: 'Wholesale', body: <BulletList items={['Flat, margin up']} /> }]} />
```
